阅读AI-README，并明确你的职责，等待下一步指令。

你需要给每个markdown文件添加前置的tag信息，每一个md文件都只能有:

description: 文字，用于描述文档的内容
keywords：列表，用于描述文档的关键词，10-20个，用于提升SEO
properties：可以是任何合法的yaml，用于描述文档的属性

你需要对每个文档都这样做

每次用户提问的更多信息都需要被合理的插入文档。